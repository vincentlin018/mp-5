// Title component for the main heading of the URL shortener page
export default function Title() {
    return (
        // Render a large, bold, centered heading with margin at the top and bottom
        <h1 className="text-3xl font-bold mb-4 text-center mt-8">
            Create Your Short URL
        </h1>
    );
}

